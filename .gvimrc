set columns=140
set lines=40

if has("gui_running")
    if has("gui_win32")
        set guifont=DejaVu_Sans_Mono_for_Powerline:h9
    else
        set guifont=DejaVu\ Sans\ Mono\ for\ Powerline\ 9
    endif
endif
